process {
    withName: '.*ANNDATA_TOH5AD' {
        ext.args   = '--delimiter "___"'
        ext.prefix = { "${meta.id}_gex"}
        cpus       = 4
        queue      = 'normal'
        memory     = { 30.GB + 30.GB * (task.attempt - 1) }
    }
}